# 完整复现入口

本版新增可独立运行的完整复现流程，请先阅读 [复现说明](复现说明.md)，执行 `python -X utf8 reproduce.py --out ../reproduced --jobs 2`。仅 `support/launch.py smoke` 不能验证数值结果。

# 支撑材料说明

## 1. 包内结构与论文附录的对应关系

`support/` 与论文附录中的匿名源码路径一致（`support/project/...`），包含四问求解、验证与后处理源码、题面输入工作簿、依赖清单与快照哈希表。运行时先执行 `python support/launch.py smoke`，该入口只做语法、哈希与 `--help` 检查，不积分；数值复现命令见 `support/README.md`。

`figures/` 保留论文正文引用图表时的相对路径；`literature/` 为自主查阅的文献资料。

## 2. result2 的两个文件

本题第二问要求逐 1 s、径向每 0.1 cm 保存完整结果。全程工作簿共 206907 行 × 21 个位置 × 2 个物理量，以 xlsx 序列化后为 26.9 MB，与官方“支撑材料压缩包不超过 20 MB”的规定冲突；经三种独立序列化方式实测（原始格式重压、最小标记重写、共享字符串重写）均为 27–29 MB，这些是已测试编码方案的体积结果，不构成所有编码方式的理论下界。因此本包按以下方式提供，数值不删减：

| 文件 | 内容 | 行数 |
|---|---|---|
| `results/result2.xlsx` | 题面表格所用的前 3 h 网格，模板格式 | 10801 |
| `results/result2_全程轨迹_206907x21.npz` | 烘干全程每个 1 s 的完整网格 | 206907 |

两个文件的数据完全一致：前 3 h 的 453600 个温度与水分浓度值逐一相同（仅表头文字不同）。`results/result2_全程轨迹_206907x21.npz` 为 numba 无关的标准 `numpy` 压缩数组，键为 `time_s`、`radius_cm`、`temperature_TC`（206907×21，单位 ℃）、`moisture_C`（206907×21，单位 kg/kg），均为四位小数。使用包内 `support/project/q4_complete_delivery/v1/source/package_workbooks.py` 可从同一轨迹重新导出全程 xlsx 工作簿。

## 3. 完整性

包内每个数值都与正式工作簿逐值核对：前 3 h 表的 453600 个数据值与本包全程轨迹数组一致；论文表 3、表 4 的全部数值均可在 `results/result2.xlsx` 中定位到同一单元格。文件列表见 `支撑材料文件列表.md`。

## 4. 匿名性

包内不含参赛者、学校、赛区信息，不含承诺书与编号专用页。源码使用匿名相对路径；自动扫描结果见构建证据。

## 5. 未纳入本包的较大材料

$120$ MB 的第三方文献 `foods-11-04045-v2.pdf` 因体积超出上限未纳入，其引用信息见论文参考文献。论文电子稿按官方要求单独提交，不重复放入本包。
