# 项目协作约定

## 实质更新后自动同步 GitHub

用户已于 2026-09-11 明确授权：以后项目一旦有实质更新，就同步到 GitHub。这是本项目的持续授权，正常提交与推送不需要再次询问。

- 仓库：`https://github.com/Hel10o/CUMCM26.git`（2026-09-14 由 `Hel10o/libai` 改名而来，旧名由 GitHub 重定向），日常同步目标为 `origin/main`。
- 在每次项目工作开始时检查本地变化；完成一个可交付阶段后，将本轮成果及已核查的用户新增材料提交并推送，然后再向用户报告完成。
- 实质更新包括模型与代码、输入或正式结果、独立复算与审核证据、论文、交接提示词、项目说明及配置的有效变化。
- 每次向用户交付新的Pro提示词或交接材料前，先完成本轮GitHub同步，核对远程提交与本地一致并读回关键入口，再报告可发送给Pro；同步失败时明确说明，不将仅本地版本称为已同步。
- 缓存、虚拟环境、临时文件、编译中间产物和未完成的运行输出不作为同步成果；正式结果及支持结论的日志、数组、图表和哈希证据应保留。
- 用户明确要求暂不上传、只在本地修改或暂停同步时，以其最新要求为准。

## 同步执行要求

1. 检查当前远程地址、分支和待提交文件；遵循 `.gitignore`，不提交凭据。核实删除项确为本次需要同步的项目变化。
2. 完成本轮修改所需的验证。仅更新文档或同步已验收成果时，不重复运行整套数值求解。
3. 更新受影响的 README 和交接入口；若修改了复审包内文件，同步更新包及其清单，确保目录与压缩包版本一致。
4. 保持已验收原始交付、审核记录和哈希证据的字节完整性，遵循 `.gitattributes`；修订数值或分析时使用明确的新版本目录，避免覆盖历史证据。
5. 获取远程最新状态。若存在他人更新，先妥善整合并验证，保留本地工作；不使用强制推送覆盖远程历史。
6. 审阅暂存内容，使用说明实际成果的提交信息，然后推送到同步目标。仅为推送而进行操作时，不额外修改模型或答案。
7. 核对远程提交与本地提交一致，并检查本轮关键文件已进入远程版本。最后回复同步结果和提交链接；失败时明确区分“已本地提交”与“已远程同步”。

若连接失败，优先检查并使用本机已配置的网络代理，不擅自改变全局网络配置。无法完成推送时保留本地成果和提交，在后续项目工作中继续同步。
